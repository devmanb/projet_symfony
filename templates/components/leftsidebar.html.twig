<div class="menu-list">
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="d-xl-none d-lg-none" href="{{ path('home') }}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav flex-column">
            <ul class="navbar-nav flex-column">
                <li class="nav-divider active">
                    <a href="{{ path('home') }}">
                        <i class="fas fa-tachometer-alt"></i> {{ 'Accueil' | trans }}
                    </a>
                </li>
                <li class="nav-divider active">
                    <a href="{{ path('mission_index') }}">
                        <i class="fas fa-random"></i> {{ 'Mission' | trans }}
                    </a>
                </li>
                <!-- <li class="nav-divider">
                    Configurations
                </li> -->
                {% if app.user.fonction.id == 1 and  app.user is not null and  app.user.fonction is not null and app.user.getFonction() is defined and  app.user.getFonction().id == 1 %}
                <li class="nav-divider active">
                    <a href="{{ path('parametre') }}">
                        <i class="fas fa-capsules"></i> {{ 'Paramètre' | trans }}
                    </a>
                </li>
                {% endif %}
                <li class="nav-divider active">
                    <a href="{{ path('calendrier') }}">
                        <i class="fas fa-capsules"></i> {{ 'Planning' | trans }}
                    </a>
                </li>
            </ul>
        </div>
    </nav>
</div>